# Evolving Machine
A Machine that evolves with every interaction that it goes through.
```bash
  echo '[{"role": "user", "content": "I have a question..."}]' \
    | uvx evolving-machine \
        --provider-api-key=sk-ant-api... \
        --github-token=ghp_... 
```
Or:
```bash
  pip install evolving-machine
```
Then:
```Python
  # Python
  import machine_name
```
